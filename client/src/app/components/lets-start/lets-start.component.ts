import {Component, ElementRef} from '@angular/core';
import 'flowbite';

@Component({
  selector: 'app-lets-start',
  standalone: true,
    imports: [],
  templateUrl: './lets-start.component.html',
  styleUrl: './lets-start.component.css'
})

export class LetsStartComponent {
/*  currentIndex = 0; // Index de l'élément actif


  updateProgress() {
    const progressBar = document.getElementById('progress-bar');
    if (progressBar) {
      const progress = ((this.currentIndex + 1) / 5) * 100;
      progressBar.style.width = progress + '%';
    }
  }*/
}
